res://
  assets/               # SOLO archivos fuente (arte/audio)
    art/
    audio/
  scenes/               # Escenas .tscn separadas por rol
    core/               # Menú, HUD, Game (root), Checkpoint, Goal
    levels/             # Niveles (Level_01, Level_02, …)
    player/             # Player.tscn + cosas del player
    enemies/            # Wolf.tscn, Snake.tscn, etc.
    items/              # Bone.tscn, PowerUps.tscn
  scripts/              # Lógica reusable en .gd
    autoload/           # Singletons: GameManager.gd, SaveSystem.gd, Signals.gd
    ui/                 # Scripts del HUD/menús (opc)
    enemies/            # Lógica de enemigos (si no va pegada a la escena)
    player/             # Player.gd (si lo separás)
    utils/              # Helpers: Constants.gd, Math.gd (opc)
  ui/                   # Fuentes, temas, íconos (si usás)
  data/                 # Configs y plantillas (no saves)
